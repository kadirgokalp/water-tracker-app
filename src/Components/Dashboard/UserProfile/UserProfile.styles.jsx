import { StyleSheet } from "react-native"

export const styles = StyleSheet.create({
	avatarContainer: {
		backgroundColor: "#6C63FF",
		borderRadius: 50,
		padding: 10,
	},
	letterText: {
		color: "#FFFFFF",
	},
})

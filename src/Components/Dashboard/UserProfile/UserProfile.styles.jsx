import { StyleSheet } from "react-native"

export const styles = StyleSheet.create({
	avatarContainer: {
		backgroundColor: "#6e0442fe",
		borderRadius: 50,
		padding: 10,
	},
	letterText: {
		color: "#FFFFFF",
	},
})
